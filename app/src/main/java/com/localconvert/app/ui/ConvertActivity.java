package com.localconvert.app.ui;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import com.localconvert.app.R;

/**
 * Reserved for a future dedicated conversion screen
 * (e.g. batch convert, advanced options, progress detail).
 */
public class ConvertActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // TODO: inflate a dedicated layout when needed
    }
}
